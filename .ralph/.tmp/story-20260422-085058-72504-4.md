### LLR-004: Add Local Runtime Probe Logic
Status: in_progress
Depends on: LLR-003

Description:
Add reusable probe logic that can test whether a configured local runtime is reachable and expose model/server metadata where possible.

Acceptance Criteria:
- [ ] Implement non-streaming probe functions for OpenAI-compatible llama.cpp endpoints and Ollama.
- [ ] Probe requests must use bounded timeouts and predictable error objects.
- [ ] Add tests using mocked fetch for reachable, unreachable, malformed, and timeout cases.
- [ ] Do not require a live local model server in the default test suite.
- [ ] Run targeted tests plus pnpm -s typecheck.
